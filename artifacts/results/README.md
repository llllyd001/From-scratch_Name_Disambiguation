# Result Artifacts

Raw LLM responses and full experiment outputs are intentionally excluded from
version control because they are large, provider-specific, and reproducible only
with the corresponding dataset and API access. `portfolio/` contains the small,
auditable result summary used by the repository overview.
